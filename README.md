# React-Login-Status-App
A simple React application that demonstrates state management using the useState hook. It shows login and logout status through conditional rendering and updates the UI dynamically when the button is clicked.
